import React from "react";
import ReactDOM from "react-dom/client";
import Play2WinHub from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Play2WinHub />
  </React.StrictMode>
);
